export function HistoricalDraftApproval({outboxId,gameId,csrf}:{outboxId:string;gameId:string;csrf:string}) {
  return <details className="small"><summary>Publish this historical game once</summary><p>This explicit exception applies only to {gameId}. It uses the latest complete report and current post template; an older saved preview may differ. Existing published or uncertain posts cannot be replaced.</p><form action="/api/admin/action" method="post" className="admin-form"><input type="hidden" name="action" value="approve-historical-draft"/><input type="hidden" name="csrf" value={csrf}/><input type="hidden" name="outboxId" value={outboxId}/><label className="checkbox-label"><input type="checkbox" name="confirmHistoricalInitial" value="yes" required/> I authorize one initial post for this historical game to the selected X account.</label><button className="button outline">Approve this historical game</button></form></details>;
}
