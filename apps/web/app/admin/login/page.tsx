import type { Metadata } from "next";
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { getSession } from "@under-review/core/auth";
export const dynamic = "force-dynamic";
export const metadata: Metadata = {title: "Operator sign in", robots: {index:false,follow:false}};
export default async function Login({searchParams}: {searchParams: Promise<{error?: string}>}) { let session=null; try {session=await getSession((await cookies()).get("ur_session")?.value);} catch {} if(session) redirect("/admin"); const query=await searchParams; return <div className="login-wrap"><div className="login-editorial"><p className="eyebrow">THE OPERATOR DESK</p><h1>Keep the<br/>record honest.</h1><p>Source checks. Independent review.<br/>Deliberate publication.</p></div><section className="login-panel"><span className="eyebrow">AUTHORIZED OPERATORS</span><h2>Sign in</h2><p className="small muted">Use the account created by your deployment operator.</p>{query.error && <div className="notice" role="alert">Sign-in was unsuccessful. Check your credentials and try again.</div>}<form action="/api/auth/login" method="post" className="admin-form"><label>Username<input name="username" autoComplete="username" required maxLength={100} autoFocus/></label><label>Password<input name="password" type="password" autoComplete="current-password" required maxLength={1024}/></label><button className="button dark" type="submit">Sign in →</button></form><p className="small muted">Administrative actions are authenticated and audited. No public account registration.</p></section></div>; }
